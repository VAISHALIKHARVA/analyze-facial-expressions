uvicorn main:app --reload

python -m venv env
env\Scripts\activate

pip freeze > requirements.txt
pip install -r requirements.txt

pip install streamlit google-generativeai pillow opencv-python python-dotenv